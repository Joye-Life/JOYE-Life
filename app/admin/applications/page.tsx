import { createAdminClient } from "@/lib/supabase/admin";
export const dynamic="force-dynamic";
export default async function ApplicationsPage(){let applications:any[]=[];try{const {data}=await createAdminClient().from("beta_applications").select("id,full_name,email,primary_focus,expected_frequency,status,created_at").order("created_at",{ascending:false}).limit(50);applications=data||[]}catch{}return <section className="shell py-10"><p className="eyebrow">Admin</p><h1 className="mt-3 text-4xl font-semibold tracking-tight">Beta applications</h1><div className="card mt-8 overflow-hidden"><div className="overflow-x-auto"><table className="w-full text-left text-sm"><thead className="bg-mist text-black/50"><tr><th className="p-4">Applicant</th><th className="p-4">Focus</th><th className="p-4">Usage</th><th className="p-4">Status</th></tr></thead><tbody>{applications.length?applications.map(a=><tr key={a.id} className="border-t border-black/5"><td className="p-4"><p className="font-semibold">{a.full_name}</p><p className="text-black/45">{a.email}</p></td><td className="p-4">{a.primary_focus}</td><td className="p-4">{a.expected_frequency}</td><td className="p-4 capitalize">{a.status}</td></tr>):<tr><td className="p-8 text-center text-black/45" colSpan={4}>No applications yet, or admin credentials are not configured.</td></tr>}</tbody></table></div></div></section>}
